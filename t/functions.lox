fun hello(name) {
  print "Hello, " + name + "!";
}

hello("Justin");

fun sayHi(first, last) {
  print "Hi, " + first + " " + last + "!";
}

sayHi("Dear", "Reader");

fun add(x, y) {
  return x + y;
}

print add(2, 3);
